// Generated by CoffeeScript 1.10.0
(function() {
  var all, byType, deparse, first, parse, ref, tables, walk;

  parse = require('pg-query-native').parse;

  deparse = require('./deparser').deparse;

  ref = require('./utils'), walk = ref.walk, all = ref.all, first = ref.first, tables = ref.tables, byType = ref.byType;

  module.exports = {
    parse: parse,
    deparse: deparse,
    walk: walk,
    first: first,
    all: all,
    tables: tables,
    byType: byType
  };

}).call(this);
