import * as t from '@babel/types';
import { NodePath } from '@babel/traverse';
import { ParserScope } from '../../scope';
export declare const getConfigOptions: (path: NodePath<t.ExpressionStatement>, scope: ParserScope) => {};
